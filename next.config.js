// This file exists only for Hostinger auto-detection
// The actual Next.js app is in apps/web
// Build commands use the monorepo structure

module.exports = {
  // This config is not used - actual config is in apps/web/next.config.ts
  // Hostinger will detect Next.js from this file and package.json
  // But we use custom build commands that build from root
};
