# Copy to `.env.local` (gitignored). Prisma + Firebase for admin API routes.

# Database — usually already in repo root `.env`; loading root env is enabled in next.config.ts
# DATABASE_URL="postgresql://..."

# Firebase Admin (same JSON as portal) — path relative to apps/admin
FIREBASE_SERVICE_ACCOUNT_PATH=../../infintysports-62c45-firebase-adminsdk-fbsvc-425819d52b.json
