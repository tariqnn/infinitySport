ALTER TABLE "Event"
  ADD COLUMN IF NOT EXISTS "slug" TEXT,
  ADD COLUMN IF NOT EXISTS "endAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "videoUrl" TEXT,
  ADD COLUMN IF NOT EXISTS "galleryUrls" JSONB,
  ADD COLUMN IF NOT EXISTS "contentType" TEXT NOT NULL DEFAULT 'GALLERY',
  ADD COLUMN IF NOT EXISTS "registrationUrl" TEXT;

CREATE UNIQUE INDEX IF NOT EXISTS "Event_slug_key" ON "Event"("slug");
